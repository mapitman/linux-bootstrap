#!/usr/bin/env bash
# source <(curl -fsSL https://raw.githubusercontent.com/mapitman/linux-bootstrap/initial-dev/generic/github-auth-login)

echo "Auth/Login for GitHub CLI. Skip SSH key upload."
gh auth login --git-protocol ssh --web --scopes "admin:public_key"
